import { UserProfileDTO } from "common/models/UserProfileDTO";
import { API } from "../api";

// export const getUserProfileApi = (id: string): Promise<any> => API().get(`my-profile/${userId}?id=${id}`);

// export const updateUserProfileApi = (id: string, newUserProfile: UserProfileDTO): Promise<any> => API().put(`my-profile/${userId}?id=${id}&newUserProfile=${newUserProfile}`);
